/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EnumPipe } from './enum.pipe';

describe('Pipe: Enume', () => {
  it('create an instance', () => {
    let pipe = new EnumPipe();
    expect(pipe).toBeTruthy();
  });
});
